# Cloudflare Pages configuration for piq.dev
#
# This project combines:
# - VitePress (static docs at /, /guide/*, /reference/*)
# - Astro (prerendered /playground, SSR /api/*)

name = "piq-website"
compatibility_date = "2024-01-01"

pages_build_output_dir = "./dist"
