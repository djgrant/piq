{
  "name": "piq",
  "private": true,
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "playground",
    "website",
    "docs"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:site": "./scripts/build.sh",
    "test": "bun test",
    "check": "bun run --filter '*' check",
    "clean": "rm -rf packages/*/dist dist"
  },
  "devDependencies": {
    "@pokit/core": "^0.0.42",
    "@pokit/prompter-clack": "^0.0.42",
    "@pokit/reporter-clack": "^0.0.42",
    "@pokit/tabs-ink": "^0.0.42",
    "@types/bun": "latest",
    "bumpp": "^10.3.2",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3",
    "zod": "^4.3.5"
  },
  "dependencies": {
    "wrangler": "^4.50.0",
    "yaml": "^2.8.2"
  }
}
