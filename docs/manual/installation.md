# Installation

```sh
npm install piqit @piqit/resolvers zod
```

`piqit` exports the query builder and type system.

`@piqit/resolvers` exports resolver factories for Bun filesystems and precompiled datasets.
